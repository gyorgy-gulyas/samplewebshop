global:
  scrape_interval: 5s

scrape_configs:
  - job_name: sales
    # The service serves this itself - there is nothing to install in it for Prometheus to work.
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:5000"]
